{% extends "/layouts/base.twig" %}

{% block content %}
	<div class="container">
		<div class="row">
			<div class="col-md-12">
				<h1>{{ post.title }}</h1>
				<p>{{ post.content }}</p>
			</div>
		</div>

		<h1>{{ home_main_title }}</h1>
		<p>{{ home_main_description }}</p>

		<p>{{ home_texto_adicional }}</p>
		{{post}}
		{{ post_by_id }}
	</div>

{% endblock %}
